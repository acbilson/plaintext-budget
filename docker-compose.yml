version: '3.3'
services:
  web:
    image: ptb-web:12.11.1
    build: 
      context: ./web
    ports: 
      - "4200:80"
  server:
    image: ptb-server:2.1
    build: 
      context: ./service
    ports:
      - "5000:80"
    volumes: 
      - ./logs:/app/logs
